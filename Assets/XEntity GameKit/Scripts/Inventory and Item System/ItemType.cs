namespace XEntity.InventoryItemSystem
{
    //NOTE: It is not recommanded to remove the default type.

    //This enum holds the types of items.
    //Add custom item types here.
    public enum ItemType
    {
        Default,
        ToolOrWeapon,
        Placeable,
        Consumeable
    }
}
